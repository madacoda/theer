// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id         Int      @id @default(autoincrement())
  uuid       String   @unique @default(uuid())
  name       String
  email      String   @unique
  password   String
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  roles            UserRole[]
  created_tickets  Ticket[]   @relation("CreatedTickets")
  resolved_tickets Ticket[]   @relation("ResolvedTickets")

  @@map("users")
}

model Role {
  id          Int     @id @default(autoincrement())
  uuid        String  @unique @default(uuid())
  name        String
  description String?

  users UserRole[]

  @@map("roles")
}

model UserRole {
  id   Int    @id @default(autoincrement())
  uuid String @unique @default(uuid())

  user_id Int
  role_id Int

  user User @relation(fields: [user_id], references: [id])
  role Role @relation(fields: [role_id], references: [id])

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@unique([user_id, role_id])
  @@map("user_roles")
}

model TicketCategory {
  id          Int      @id @default(autoincrement())
  uuid        String   @unique @default(uuid())
  title       String
  description String?
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  tickets Ticket[]

  @@map("ticket_categories")
}

model Ticket {
  id          Int             @id @default(autoincrement())
  uuid        String          @unique @default(uuid())
  category_id Int?
  category    TicketCategory? @relation(fields: [category_id], references: [id])
  title       String
  content     String? @db.Text

  status          String   @default("open") // open, processed, resolved, closed
  sentiment_score Float?
  urgency         String?  // High, Medium, Low
  ai_draft        String? @db.Text
  ai_metadata     Json?    // Stores processing time, retry count, etc.
  
  created_by_id   Int?
  created_by      User?    @relation("CreatedTickets", fields: [created_by_id], references: [id])
  
  resolved_by_id  Int?
  resolved_by     User?    @relation("ResolvedTickets", fields: [resolved_by_id], references: [id])
  resolved_at     DateTime?

  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  @@map("tickets")
}
